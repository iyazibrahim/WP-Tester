# App / API runtime only.
# Scanner CLIs (wapiti, droopescan, etc.) live in /opt/scanner-venv — they must NOT
# share this environment: wapiti3→mitmproxy pins Flask<2.3 while the app needs Flask 3.
colorama>=0.4.6
Click>=8.0.0,<9
Flask>=3.0.0,<3.2
Werkzeug>=3.0.0,<4
Jinja2>=3.1.0,<4
itsdangerous>=2.2.0,<3
blinker>=1.9.0,<2
MarkupSafe>=2.1.1,<4
Flask-Limiter>=3.5.0,<4
gunicorn>=21.2.0,<24
